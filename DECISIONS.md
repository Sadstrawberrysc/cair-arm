# 决策日志

本文件只记录影响多个模块、公共接口、运行安全或长期维护方式的关键决策。详细设计放在
`ARCHITECTURE.md` 和各模块文档中。

后续记录规则：

- 新决策按时间倒序追加，格式保持为“日期 / 决策 / 原因”。
- 已落地决策发生变化时新增一条“取代某决策”的记录，不改写历史。
- 参数微调、普通修复、一次性实验和实现细节不记入本文件。
- 尚未确定的方案写入 `PROGRESS.md` 待办，达成结论后再转为决策。

## 2026-09-07：arm_probe_pose 关节预警裕量调整为3°

**决策：** 按用户明确要求，将 `arm_probe_pose` 全部输入模式的关节限位预警裕量从5°改为3°，
取代此前5°决定。原3°硬停止门、奇异拒绝及生产默认配置不变。

**原因：** 用户要求进一步缩小预警裕量。采用构建和 J4 3.1°/2.9°裕量的离线边界检查，
验证允许区与硬拒绝仍有效；本次不执行真机，后续目标仍需重新规划检查。

## 2026-09-07：单点编排入口默认执行真机 MoveJ

**决策：** 按用户明确要求，`run_probe_target.py` 无参数时使用速度1，采样保存并正常退出后
自动向 Robot 传入 `--execute --confirm-single-movej`。提供 `--dry-run` 用于只规划。
本决定取代此前编排入口默认 dry-run 的约定，Robot 直接调用的默认模式与算法安全门不变。

**原因：** 简化每次采样运行的命令。通过模拟子进程验证无参数执行、dry-run不执行及
未保存/相机失败时不调用 Robot；本次修改不进行真机运行。

## 2026-09-07：数字人目标首点采用画面最低红点

**决策：** 按用户要求，Camera_RT 在三维反投影前按像素 y 降序排列采样点，保存的首点
及法向对应画面最低红点。单点编排继续取法向行后的第一坐标，无需改动 Robot。

**原因：** 保证用户视觉选择与自动 MoveJ 目标一致；模型内部顺序和三维 Y 不能保证该语义。
离线排序及编排测试验证后，需重新按 s 保存新快照供使用。

## 2026-09-07：arm_probe_pose 关节预警裕量调整为5°

**决策：** 按用户明确要求，仅将 `arm_probe_pose` 的 planner 关节限位预警裕量从10°改为5°，
应用于其全部输入模式的 IK 和201点 MoveJ 路径检查。生产默认10°、硬停止3°及其他门不变。

**原因：** 当前目标的 IK 在 J4=125.134°、裕量9.866°处触发原预警。验证采用构建和现有
5°预警边界离线测试；真机前需重新 dry-run 确认最终解与路径，不能从中间迭代推断可达。

## 2026-09-07：数字人采样到单次 MoveJ 使用独立编排入口

**决策：** `hand_eye_calibration-main/run_probe_target.py` 启动 `cliff_demo.py`，在正常退出且
本次采样文件更新后取第一采样点（跳过法向行），通过同目录新外参转为 Base 位置，再调用
`arm_probe_pose --target-position-base-m`。默认 dry-run；双执行参数仅传给 Robot 完成单次
MoveJ。Robot 继续拥有当前姿态读取、Probe 坐标链、IK 和所有运动安全门。

**原因：** 将用户的采样、转换和运动步骤串成一次调用，避免抄错点或重复使用未更新快照。
相机不直接控制机器人；编排入口不复制通信协议，不自动重试运动。

## 2026-09-07：数字人单点改用 hand_eye_calibration-main 外参

**决策：** 根据用户对外参来源和正确性的明确确认，数字人单点改由
`infer/hand_eye_calibration-main/transform_point.py` 使用同目录的 `d455_to_rm75_base.json`
转换到 RM75 Base，不再读取 `infer/Calibration` 外参。`arm_probe_pose` 使用
`--target-position-base-m`，把 Base 位置与连接后读取的启动 Probe TCP 姿态组成完整六维目标，
再经过现有 Probe TCP 坐标链、IK 和单次 MoveJ 安全门。该模式不附加50 mm法向偏置。

**原因：** 用户指出旧 Calibration 外参有误，并指定重新计算结果为当前正确来源。数字人只
提供三维位置，不能自行定义探头姿态；将相机点转换与 Robot 的 ArmTip/Tool/Probe 姿态组合
分开，可避免把 ArmTip 欧拉角误当作 Probe 欧拉角。新外参尚无独立验证记录，工具必须明确
输出该事实，真机仍须 dry-run 和现场核对。

## 2026-09-04：Base 颈动脉路径接入 Probe TCP 位姿工具

**决策：** `arm_probe_pose` 新增唯一的 `--artery-path-base` 路径接口，并与显式
`--target-pose-m-rad` 互斥。路径模式固定选择首点、沿单位外法向偏置50 mm，并在连接后读取
启动 Probe TCP 姿态作为目标姿态；不开放点号、偏置或姿态覆盖参数。程序从同名 sidecar
隐式定位 accepted 相机外参，并校验路径、外参、相机序列号和坐标语义。

**原因：** Base 路径只提供位置和法向，无法唯一确定完整三维姿态；当前阶段明确要求保持
现场已调整的探头姿态。将固定策略放在接口内部可避免运行命令重复输入外参路径或产生互相
矛盾的点号、距离和姿态参数，同时继续保留默认 dry-run、IK、限位、奇异和整段检查。

## 2026-09-04：Probe TCP 绝对位姿维护工具使用单次关节运动

**决策：** 新增的 `arm_probe_pose` 维护工具将输入明确解释为 RM75 Base 坐标系下的
`Base -> Probe_TCP` 位姿（位置为米，控制器 ZYX 欧拉角为弧度），经唯一的
`CalibratedFrameChain` 换算 Arm_Tip 目标和七轴数值 IK 后，只允许提交一次 `MoveJ`。
默认只规划不运动；真机执行必须同时提供 `--execute --confirm-single-movej`，并要求
`tool_chain_verified=true`，同时保留模型一致性、关节限位、奇异性、关节行程和最终误差门。

**原因：** 探头末端目标不能直接作为控制器 Arm_Tip 位姿发送，否则会漏掉安装旋转和 TCP
偏移；显式单位、坐标语义和双重执行确认也可降低维护命令误用为真机运动入口的风险。

## 2026-09-04：取消单点 MoveJ 最大关节变化拒绝门

**决策：** 根据用户明确要求，`arm_preset_pose` 颈动脉模式不再因最大关节变化超过 30°拒绝
IK 或 MoveJ，实际最大变化继续输出。该变更不影响工具的预置/手动关节目标模式，也不影响
生产 `main_rm75`。5°限位预警、3°硬停止、硬限位、奇异拒绝、IK收敛、0.6 mm残差、201点
关节路径检查和2 mm终点门保持不变。

**原因：** 用户希望允许单点目标使用更大的关节空间变化。系统没有环境碰撞模型，取消该门
会扩大机械臂扫掠范围，因此不构成路径安全证明；执行前必须由现场人工确认整段净空。

## 2026-09-04：取消单点 MoveJ 当前距离拒绝门

**决策：** 根据用户明确要求，`arm_preset_pose` 颈动脉模式不再因当前 Probe TCP 到目标的
距离拒绝 IK 或 MoveJ；距离继续以毫米输出。50 mm 外法向悬停偏置、30°最大关节变化、5°
限位预警、3°硬停止、奇异拒绝、0.6 mm IK 残差、201 点关节路径检查及2 mm终点门保持不变。

**原因：** 用户希望从当前姿态直接规划单次 MoveJ，并明确取消此前 100 mm 距离门。当前系统
没有人体、床体、支架或线缆的环境碰撞模型，因此本变更不表示长距离路径安全；执行前必须
人工确认整段关节空间运动净空。本决定取代下方 100 mm 距离门决定。

## 2026-09-04：单点维护模式关节限位预警裕量调整为 5°

**决策：** 根据用户明确要求，仅将 `arm_preset_pose` 颈动脉模式的关节限位预警裕量从默认
10°调整为 5°。生产 `main_rm75` 的 planner 默认值不变；3°硬停止门、硬关节限位、奇异拒绝、
30°最大关节变化和 MoveJ 201 点整段检查均保持。

**原因：** 最新 IK 的 J6 目标为 `-118.245°`，相对 `-128°` 下限尚有 `9.755°`，只触发原
10°预警而未触发3°停止门。局部调整满足用户要求，同时避免扩大到生产闭环或取消硬拒绝。

## 2026-09-04：单点 MoveJ 当前距离门调整为 100 mm

**决策：** 根据用户明确要求，将 `arm_preset_pose` 颈动脉模式中“当前 Probe TCP 到目标”的
固定距离门从 50 mm 调整为 100 mm。第一点外法向悬停偏置仍为 50 mm；最大关节变化 30°、
201 点 MoveJ 关节路径限位/奇异检查、IK 规划残差 0.6 mm 和最终位置误差 2 mm 均不改变。

**原因：** 最新目标距离为 94.43 mm，原距离门会在 IK 前拒绝。用户明确接受扩大距离范围；
保留其余规划与执行门，使调整只影响这一项距离准入，不授权或自动触发真实运动。

## 2026-09-04：记录当前 Probe TCP/Tool 链现场确认

**决策：** 用户明确确认 ArmTip→Probe TCP 的 188 mm 长度和安装旋转真实有效，并授权将当前
部署 `rm75_force_calibration.json` 的 `tool_chain_verified` 写为 `true`。该确认范围只覆盖
Probe TCP/Tool 几何链，不代表力标定残差、相机外参空间精度或任何 MoveJ 已获验收。

**原因：** 单点 IK 必须使用明确验收的末端几何链。分开记录几何链与力残差，可以解除对应
数据门而不掩盖 Force max `1.2488 N` 和当前目标距离 `480.1 mm` 等其余阻塞。

## 2026-09-04：颈动脉目标改为一次快照和单次 MoveJ

**决策：** 用户澄清不需要实时跟踪后，移除 Camera/Calibration/Robot 的实时目标 Redis
链路。Camera_RT 按 `s` 保存快照，Calibration 离线生成带外参及内容摘要的 Base 路径，Robot
现有 `arm_preset_pose` 维护工具固定取第一点并沿外法向偏置 50 mm，保持当前 Probe TCP 姿态
求解关节目标。默认只 dry-run；执行须双参数确认且最多发送一次 MoveJ。

**原因：** 固定目标无需时序、滤波和会话协议。文件摘要能够保留来源证据，独立维护入口也
避免改动生产扫描协议。执行仍受距离、IK、限位、奇异和现场授权等硬门约束。本决定取代下方
“颈动脉实时首点采用隔离 Redis 链路”决定。

## 2026-09-04：颈动脉实时首点采用隔离 Redis 链路且只开放 dry-run

**决策：** Camera_RT 发布带设备/时效的相机坐标首点，Calibration 使用 accepted
`T_base_camera` 校验、5帧中值、低通和50 mm外法向偏置后发布 `robot:artery_target:v1`。
Robot 只在显式 `--dry-run-control --target-track` 中读取真实关节并运行10 mm/s影子 IK；该模式
在参数层拒绝 execute，目标失效或规划失败锁存到新会话。

**原因：** 现有 y/rz 扫描协议不能表达 Base 三维绝对目标；独立协议可避免改变生产控制语义。
当前 `tool_chain_verified=false`，因此只允许验证坐标、时序与可达性，不允许发送运动命令。

## 2026-09-03：Base坐标颈动脉结果采用三文件本机产物组

**决策：** `infer/Calibration/data/artery_path_base.txt` 是当前 RM75 Base 坐标系下的离线
颈动脉采样点结果；必须与 `artery_path_base.txt.json` 和 `d455_to_rm75_base.json` 一起保留。
元数据必须匹配当前外参 SHA-256、相机序列号和 `rm75_base` frame。该产物组不移动到
Camera_RT、不提交 Git，也不构成 Robot 运动输入接口。

**原因：** 相机原始路径、坐标转换证据和 Base 派生路径需要保持可追溯且职责分离；只移动
一个文本文件会丢失设备、外参和来源身份，并可能把陈旧或错误坐标直接误认为可执行轨迹。

## 2026-09-03：外参验收分离PnP图像质量与坐标链空间残差

**决策：** Calibration 使用每帧 `T_camera_board` 对原始角点的 PnP 重投影误差作为 `<1 px`
图像观测门；使用 eye-to-hand 两侧坐标链的平移 RMS/最大值和旋转 RMS 作为外参门。
由机器人链预测棋盘角点产生的像素差单独报告为诊断指标，不参与接受判定。

**原因：** 当前 5.32 mm 小棋盘距相机约 6 cm，毫米级机器人/链路空间差会放大为十几像素；
把该值误当作单帧 PnP 质量会重复且距离相关地约束同一空间误差。分离指标保留异常可见性，
同时避免用像素尺度替代已经明确为毫米和角度的外参验收门。

## 2026-09-03：固定相机外参由独立 Calibration 模块持有

**决策：** `infer/Calibration` 是 `D455 color optical → RM75 Base` 外参的唯一生成边界。
它使用 D455 工厂内参、末端棋盘格和人工/可信只读提供的 `Base → ArmTip` 静止位姿进行
eye-to-hand 求解与独立验证；不复制 RM75 TCP 协议、不控制机器人。只有带相机身份、输入摘要
和独立误差报告且 `accepted=true` 的结果才允许离线转换 Camera_RT 路径。

**原因：** Camera_RT 应保持相机坐标感知职责，Robot 应保持唯一硬件通信与运动安全边界。
将标定、验收和转换隔离，可避免旧 `cam2rbt`、坐标方向错误或未经验证的矩阵直接进入控制链。

## 2026-09-02：Camera_RT 路径止于相机坐标并隔离旧机输出

**决策：** Camera_RT 当前只提供 RealSense 光学坐标中的临时路径，不把
`artery_path.txt` 或历史命名的 `artery_path_rbt.txt` 定义为机器人轨迹。两份初始提交的
旧机器输出迁入模块测试夹具，运行输出不再跟踪。固定相机未来通过独立 eye-to-hand 标定建立
`T_base_camera`；外参验证前不允许 RM75 消费该路径。

**原因：** 相机已更换机器和安装位置，旧路径与旧外参均失效；仓库也没有可信的
`artery_path_rbt.txt` 生成器、设备身份、坐标 schema 或残差证据。先固定感知服务边界并分离
静态、设备、功能和标定验收，可以防止名称含混的旧数据直接进入运动控制。

## 2026-08-21：WrenchSample 只公开生产使用的 stale 判定

**决策：** `WrenchSample` 只保留基于当前 `steady_clock` 的 `IsStale(maximum_age)`；删除无人
调用的显式时刻转发方法。有效位、I/O 错误、streaming 状态、时间戳缺失/倒退、负最大年龄和
超龄条件保持原顺序与语义。

**原因：** 全仓库只有当前时刻入口调用转发方法，额外公开另一套入口扩大 API，但没有测试或
运行调用者，也不改变 fail-closed stale 判定所需的信息。

## 2026-08-21：传感器运行健康只通过结果和快照公开

**决策：** 删除 force sensor reader 无调用的内部运行、端口和配置 getter，以及两种 parser
无调用的缓冲区大小 getter。reader 的启动结果、最新 `WrenchSample`、`LastError()` 与 parser
离线统计接口继续保留；内部原子状态、配置和有界缓冲区不变。

**原因：** 全仓库没有这些 getter 的调用者；并行暴露内部布尔状态会与携带 stale 和 I/O
错误的快照形成第二套健康判断来源，缓冲区大小也已包含在 parser 统计快照中。

## 2026-08-21：传感器 reader 不再维护无人消费的统计镜像

**决策：** 删除 `ForceSensorReader` 的聚合统计查询、镜像状态和 parser-to-reader 复制路径；
legacy 与 Haptron parser 继续维护各自的离线统计接口。生产健康状态仍通过最新
`WrenchSample` 的 sequence、checksum、stale、I/O 状态/错误以及 `LastError()` 表达。

**原因：** 全仓库没有 reader 聚合统计调用者，镜像只在串口热路径增加复制、互斥锁和公共
API 面积，却不参与任何控制、安全、日志或故障判断。保留 parser 统计即可维持协议测试 seam。

## 2026-08-21：ContactEstimate 只保留被运行链消费的结果

**决策：** `ContactEstimate` 不再公开无人读取的 STL 面片索引；退化面片只在事务式
`LoadSTL()` 边界拒绝，不再保留无法从周期估计产生的退化模型错误项。其后错误码显式保持
原数值，点、残差、等效点误差、有效位以及 Redis/CSV 错误字符串契约不变。

**原因：** 全仓库审计确认面片索引只有写入没有读取，成功加载的私有模型也不存在退化面片，
保留这两项只会扩大公共 API 并暗示不存在的运行分支。

## 2026-08-21：生产 transport 不再兼容旧六轴无返回值 API

**决策：** 删除 `RMCommand` 的 `ConnectTCPSocket()`、`Read*()`、`Move*()`、`ServoJ()`、
`HoldMotion()` 和 `StopMotion()` 无返回值 wrapper；生产与 maintenance 调用统一使用返回
`RMResult` 的 `Try*` API。`tests/legacy/six_axis` 仅保留历史源码，不要求针对生产头文件编译。

**原因：** 全仓库审计确认生产入口和 maintenance tools 均已迁移，wrapper 只会吞掉结构化
错误并保留旧六轴源码兼容负担。统一显式结果传播可以避免运动或 Stop 失败被调用者忽略。

## 2026-08-21：RMCommand 不再公开 transport 实现状态

**决策：** `RMCommand` 的 socket、收发缓冲、JSON/命令缓存及内部错误缓存全部私有化；生产
入口和 maintenance tools 只使用 `Try*`、状态快照、连接配置及 `SetQuiet()` 公共接口。

**原因：** 这些字段没有非 legacy 外部读取者，公开后却允许调用者绕过 mailbox、锁和结果传播
直接改变通信状态。收窄接口可以固定单一 I/O owner 边界，同时不改变报文、时序或运动语义。

## 2026-08-21：RM75 连接端点改为构造时单次注入

**决策：** `RMConnectionConfig` 唯一表达 RM75 控制器 IPv4 地址和端口，并在构造
`RMCommand` 时单次注入；生产入口和 maintenance tools 不再直接改写 transport 的公开字符
缓冲区或端口字段。默认端点仍为 `192.168.50.254:8080`。

**原因：** 构造后逐字段复制导致生产和维护入口重复维护缓冲区长度与终止符，并允许连接生命
周期内意外改变目标控制器。不可变配置收缩了公共 API，同时不改变连接时序、socket 协议或运动
命令路径。

## 2026-08-20：Stop 命令确认与物理静止确认分层

**决策：** `realman_transport` 唯一提供 Stop mailbox 优先级、`RequestConfirmedStop()` 有界确认
重试和 `BestEffortStopGuard` 提前退出兜底；依赖坐标的 `StopAndConfirmStationary()` 使用
`CalibratedFrameChain` 检查连续 5 帧关节、Probe TCP 和姿态静止。入口只处理返回的 `RMResult`。

**原因：** `arm_stop=true` 只证明控制器确认命令，不能证明机械臂物理静止。分层后所有启动、
故障、deadline 和正常退出路径复用同一门，同时保持原超时、重试、阈值和 Stop 高于 ServoJ 的
优先级不变。

## 2026-08-20：标定坐标链统一为不可变对象

**决策：** `CalibratedFrameChain` 从单份已校验 `ForceCalibration` 构造，唯一提供
Base→Arm_Tip→Tool/Sensor→Probe TCP 的运行时变换，包括 Probe TCP Base 位置、Tool-Y Base
方向、Base←Tool/Sensor 旋转和 Arm_Tip 姿态差。入口与安全检查不再手工拼接这些矩阵。

**原因：** 同一旋转和 TCP 偏移此前分散用于 tare、wrench 补偿、tracking、行程门和日志，重复
表达容易产生乘法顺序或坐标语义分叉。不可变对象确保各消费者使用同一标定快照，同时保持原有
Euler 约定、乘法顺序和 SI 单位。

## 2026-08-20：运行日志由独立模块单线程持有文件

**决策：** `rm75_runtime_logging` 唯一拥有异步 CSV writer、固定周期行序列化和 summary v2
构造/落盘。控制循环只提交有界周期快照，入口在停止所有 I/O owner 后只提交一次最终运行结果；
CSV header、字段顺序、单位及 summary schema v2 继续作为兼容契约。

**原因：** 日志格式化、JSON 构造和磁盘 I/O 不应进入 10 ms 控制编排，也不应长期占据生产
入口。集中所有日志序列化可使 schema characterization 测试直接覆盖真实 builder，同时保持
8192 行有界队列、丢行计数和后台单 writer 行为不变。

## 2026-08-20：运行配置统一为强类型单一来源

**决策：** `RobotRuntimeConfig` 统一装配入口参数及 `Rm75ControlConfig`、
`Rm75ServoPlannerConfig`、`Rm75RuntimeSafetyConfig`。删除控制/安全 CLI 覆盖，无参数 profile
只设置设备、生命周期和生产接近包络；启动校验、控制对象、终端输出和 summary 使用同一有效
配置。Redis `desired_force_n` 继续作为协议明确提供的单次命令覆盖。profile 名称不再编码目标力。

**原因：** 原 profile 用 `-3 N / 1 cm/s` 通过启动门，但实际控制对象长期执行
`-2 N / 2 cm/s`，导致校验、命名与真机行为分叉。用户明确选择保持当前实际行为，并授权将
provisional 准入校验对齐到 `-2 N / 2 cm/s`；raw wrench、tracking、IK、tare 和 deadline
等其他安全门保持不变。

## 2026-08-20：标定文件收敛为无版本唯一名称

**决策：** 当前v10来源的标定结果作为唯一部署文件，统一命名为
`rm75_force_calibration.json`，其采集溯源文件命名为 `calibration_samples.csv`。删除工作区内
旧v6/v9标定结果和v8/v9采集文件，无参数 `main_rm75` 只引用该无版本文件。

**原因：** 用户决定不再保留多个工程标定版本，避免默认路径与现场所称“当前标定”
不一致。该命名决策不改写质量事实：文件仍为 `residuals_verified=false`、
`tool_chain_verified=false`，Force max `1.2488 N` 仍未通过 `0.6 N` 门。

## 2026-08-20：默认临时力标定切换为v10

**决策：** 无参数 `main_rm75` 默认加载
`rm75_force_calibration_v10_provisional.json`，取代v9作为当前工程调试标定。v10仍保持
`residuals_verified=false` 和 `tool_chain_verified=false`，并继续受现有provisional启动tare、
wrench硬门和其他真机安全门约束。

**原因：** v10的10姿态采集通过多样性预检，Force max从v9的 `1.7448 N`改善为
`1.2488 N`，但仍高于正式 `0.6 N` 接受门；因此只能按用户要求作为临时调试标定，
不得表述为已验收标定。

## 2026-08-20：SensorMonitor 自动选择六维力数据源

**决策：** SensorMonitor 自动选择数据源：`main_rm75` 已发布遥测或已独占串口时，
只读订阅 Redis，以实线显示 Sensor-frame 原始 wrench、以虚线显示 Tool-frame 补偿 wrench；
否则通过独占、只读的 Haptron Modbus RTU 查询只显示原始 wrench。不提供数据源手动切换参数。

**原因：** 启动 tare 失败时需要在控制进程未运行的情况下独立观察传感器数据；明确区分原始
直连数据和生产控制器补偿数据，可以避免为诊断而启动机器人控制，也避免伪造补偿语义。

## 2026-08-20：建立仓库级协作、架构、进度和决策文档

**决策：** 根目录使用 `AGENTS.md` 约束协作和真机安全，使用 `ARCHITECTURE.md` 描述当前
服务/API 边界，使用 `PROGRESS.md` 记录实时状态和待验收项，使用本文件保存长期关键决策。

**原因：** 运行说明、架构现状、项目进度和历史决策具有不同更新节奏；分开维护可以避免
临时参数、设计原因和操作规则互相覆盖，也便于新成员找到当前事实来源。

## 2026-08-05：视觉闭环沿用三阶段协议并在视觉端映射五分类模型

**决策：** ConvNeXt 五分类 `pre/in/after/brench/rota` 在视觉服务边界映射为 RM75 的
`Scan/Trigger/Action` 三阶段；机器人控制状态机不直接依赖模型的五个训练标签。

**原因：** 保持已部署 Redis/控制状态机协议稳定，同时允许视觉模型独立迭代；模型标签变化
不会直接扩大机器人端状态空间。

## 2026-07-31：生产入口唯一化，legacy 与维护工具隔离

**决策：** RM75 生产构建只保留 `main_rm75` 入口；旧六轴源码移动到
`infer/Robot/tests/legacy/six_axis/`，诊断和标定程序移动到 `tests/tools/`，并通过默认关闭的
`BUILD_MAINTENANCE_TOOLS` 单独构建。

**原因：** 避免旧六轴入口或会运动的诊断程序被误当作七轴生产控制器，同时保留历史参考和
现场维护能力。

## 2026-07-24：Redis 视觉命令采用会话、递增序号和失效关闭

**决策：** 生产命令使用 Redis v1 payload；每个视觉进程创建新的 `session_id`，`sequence`
在会话内严格递增并以心跳刷新。断线、过期、重放、无效 JSON 或反馈恢复后的旧命令均进入
Hold，只有更新命令才能重新释放运动。

**原因：** Pub/Sub 不提供持久执行确认，不能让重连前命令、重复角度增量或陈旧视觉结果继续
驱动机械臂；失效关闭比静默沿用上一条运动命令更安全。

## 2026-07-21：控制增量统一在 Tool/Sensor 坐标表达

**决策：** 力、接触点和 Tool-X/Y/Z/RZ 控制请求在 Tool/Sensor 坐标系表达；通过标定固定
变换映射到 Arm_Tip，再由七轴 IK 生成关节目标。公共数值使用 SI 单位，视觉协议中的 RZ 度数
只在接口边界转换。

**原因：** 扫描方向和力反馈必须跟随探头物理轴，而不是 Base 轴；明确坐标链和单位可避免
安装旋转、TCP 偏移和隐式单位换算造成方向错误。

## 2026-07-21：确定性控制、关节规划和硬件 I/O 分层

**决策：** `Rm75ControlLaw` 只根据快照产生状态与笛卡尔目标，`Rm75ServoPlanner` 负责
6×7 IK、限位、速度/加速度和奇异性检查，`RMCommand`/`RMStateReader` 才拥有硬件通信。
10 ms 控制循环只消费内存快照，Redis、串口、socket 和日志写盘放在独立 I/O 线程。

**原因：** 将控制计算与非确定时延隔离，防止阻塞 I/O 破坏控制周期；同时为离线回放、单元
测试和安全规划提供清晰边界。

## 2026-07-06：机械臂主链路迁移到 RM75 七轴模型

**决策：** 生产运动学、状态和命令接口以 RM75 七关节、6×7 Jacobian 和七关节 ServoJ 为准，
不在生产链中兼容旧六轴数组或运动学假设。

**原因：** RM75 的冗余自由度、关节限位和奇异性行为与旧六轴系统不同；在生产边界保留六轴
假设会产生错误索引、错误 IK 和不可验证的运动目标。
