# 关键决策

按用户要求于 2026-09-17 合并精简；同一事项保留最终结论，关键替代关系在下表注明。
当前结构见 [ARCHITECTURE.md](ARCHITECTURE.md)，验证状态与待办见 [PROGRESS.md](PROGRESS.md)。
后续仅追加影响接口、架构或长期维护的决策，记录日期、结论与原因。

| 日期 | 决策与原因 |
| --- | --- |
| 2026-07-06～07-31 | 生产链统一 RM75 七轴；控制计算、planner 与硬件 I/O 分离，Tool/Sensor 表达控制增量，避免六轴假设与阻塞 I/O 混入闭环。旧六轴和维护工具独立。 |
| 2026-07-24～08-05 | Redis 命令采用会话、递增序号和超时 Hold；视觉端将五分类映射为既有三阶段协议，保持消费者语义。 |
| 2026-08-20～08-21 | 配置、坐标链、异步日志各用单一实现；transport 使用结构化 Try* 结果，连接配置构造时注入；删除无人消费的公共状态和统计镜像，减少重复与语义分叉。Stop 命令确认与物理静止判定分开。 |
| 2026-08-20 | SensorMonitor 自动选择 Redis 遥测或独占串口原始数据，支持控制进程未运行时诊断。 |
| 2026-08-20、09-04 | 力标定统一为 rm75_force_calibration.json；Force max 1.2488 N 未通过 0.6 N 残差门。9 月 4 日用户确认 188 mm Probe TCP 几何链，仅更新几何确认，不代表力标定验收。 |
| 2026-09-02～09-04 | Camera_RT 输出相机坐标快照，隔离旧机输出；以文件快照取代颈动脉实时 Redis 目标，简化固定初始定位。PnP 图像质量与外参空间残差分别评价。 |
| 2026-09-04～09-07 | 按用户要求取消 arm_preset_pose 颈动脉模式距离及最大关节变化拒绝门；arm_probe_pose 预警裕量最终为 3°。这些维护模式调整不改生产默认参数。 |
| 2026-09-07 | 初始定位改用 hand_eye_calibration-main 的 D455 外参，替代旧 Calibration 来源；快照按像素 y 降序取首点。run_probe_target.py 默认速度 1、自动传执行参数，--dry-run 只规划；Robot 直接调用仍默认不执行。 |
| 2026-09-08 | 初始定位使用控制器 MoveJ_P，避免复用实时 ServoJ 数值 IK；该分支不提供本地关节解和整段路径预检查，dry-run 只预览目标。 |
| 2026-09-08～09-11 | 最终短轴姿态直接构造：Z 朝内，X 为血管切平面投影的负向，Y=Z×X；替代长轴及附加 ±90°旋转。位置沿目标 Tool -Z 后退 50 mm，明确偏置坐标语义。 |
| 2026-09-11 | Gemini 标定采集使用 V4L2，图像与位姿按编号配对；求解显式选择数据目录。腕部外参使用 T_armtip_camera（m），不混用全局 T_base_camera；保存候选结果不代表独立验证。SDK 安装保留原 camera 环境依赖。 |
| 2026-09-14～09-15 | Gemini 数字人二维叠加实验因效果不满足需求回退；标定、SDK 与腕部观测方案保留。 |
| 2026-09-15 | 全局快照切换为左颈动脉。按参考姿态将经验 Tool X/Y 补偿折入 D455 平移，入口仅保留 -Z 后退；只在参考姿态等价，不是重新标定。原始依据见下方。 |
| 2026-09-15 | 全局交接采用 60 秒 Redis seed，Robot 发布相机位姿，腕部预览做投影与局部追踪；失败按事件保存证据，便于定位纹理/匹配问题。 |
| 2026-09-16 | 新增独立点击跟随，复用追踪与坐标链，无需全局 seed；取代“腕部仅有预览、尚无消费者”的阶段状态。观测按身份、序号、时效校验，失败 Hold，重新点击显式恢复；50 mm 偏置在腕部控制器中施加一次。 |
| 2026-09-16 | 按用户要求增加 --wrist-no-force，仅用于腕部模式，不连接力传感器、不伪造有效零力；普通模式保持原行为。 |
| 2026-09-17 | 按用户要求精简 AGENTS.md，移除原硬件边界、模块及逐功能验收限制；此次再将架构改为当前链路索引、决策改为摘要，减少重复上下文。文档重写不修改控制实现或运行配置。 |

## D455 经验修正依据（2026-09-15）

修正只改变原外参平移，增量及参考输入保留下列记录；完整原矩阵可供追溯。
该信息此前从外参 JSON 移入本文件，独立验收仍未完成。

```json
{"applied_at":"2026-09-15","method":"fixed Base translation derived from Tool X/Y offsets at the reference target orientation","original_source":"compute_to_hand.py","original_calibration_sha256":"4edf73222dd01a14c982c63c36847e01a4b61ce24e5e2b81eb401ff9733e26ae","original_T_base_camera":[[0.2652302,-0.00343359,0.964179,-0.29558649],[-0.96040425,0.08753032,0.26450354,-0.31307089],[-0.08530309,-0.99615594,0.01991805,0.10430691],[0.0,0.0,0.0,1.0]],"reference_snapshot":"infer/Camera_RT/artery_path.txt","reference_snapshot_sha256":"b03ad1a1d3a51132457cbfe0b44e0b1d2a2fb4d6e93100322038a2c57816783a","reference_camera_rows_m":[[-0.646,0.084,-0.758],[0.065139,-0.258339,0.845],[0.064808,-0.269316,0.849],[0.065624,-0.288651,0.877],[0.064104,-0.301514,0.884],[0.060358,-0.309324,0.883]],"reference_R_base_tool":[[-0.038613375430051326,0.42799614873055436,0.9029553166739266],[-0.18197556397660541,0.8855067627873922,-0.42750750539983484],[-0.9825446052741292,-0.18082331080258066,0.04369243546769873]],"absorbed_tool_offset_m":[0.05,0.1,0.0],"delta_translation_base_m":[0.040868946101552873,0.07945189807990895,-0.06720956134396454],"limitation":"Equivalent to previous Tool X/Y compensation only at the reference orientation; not a new hand-eye calibration."}
```

## 2026-09-17：腕部文档统一为三份

按用户要求，Camera_wrist 仅保留 PROGRESS.md（状态与历史证据）、ARCHITECTURE.md（结构与协议）、
USAGE.md（环境、操作与验证步骤），替代原六份文档并更新引用，减少分散维护。

## 2026-09-17 / 无力状态日志保持summary v2兼容

重读项目后补完无力模式测试，发现新增顶层force_sensor_enabled违反现有summary v2键集合。
改为control.force_sensor_enabled，并同步回归/文档；不改变CSV列集合或真实运动门。
隔离无力仿真和实机5秒observe证明不读力也可读取机器人状态，未执行真实运动。

## 2026-09-17 / 用户授权候选外参试验及取消累计位移转角门

用户确认在候选腕部外参、完全不读取力传感器的条件下真实联调，随后明确取消5mm累计位移、
5°累计转角上限。新增独立--wrist-candidate-trial和--wrist-unlimited-excursion；后者只能
与前者配合，前者必须是显式腕部无力模式，运行时间1..30秒。外参文件不改为已验证。
仅跳过该模式下实测与规划的累计位移/累计转角门，速度5mm/s、5°/s、关节/IK、跟踪误差、
通信/周期、Hold及急停仍保留。普通模式默认行为不变。
启动输出和summary control记录试验状态与累计门是否启用。7项CTest及752周期隔离仿真通过，
合成实测位移确实超过5mm，仍验证旧帧/超时/断线/显式恢复；模拟ServoJ发送0。
证据/tmp/wrist_loopback_4snorfg0。不能把候选实验当作外参或真实跟随已验收。

## 2026-09-17 / 腕部专用无参数常驻入口

用户要求无需逐项传参且Robot一直开启。新增Camera_wrist/start_robot.sh，固定用户已授权的
无力候选外参真实执行组合，使用duration=0，exec透传Ctrl+C，唯一/tmp日志目录，路径不依赖cwd。
仅候选试验允许执行duration=0，普通执行配置不变；相机b/r人工开始、失效Hold及故障退出仍保留。
相机q结束本轮但不退出Robot，不做自动重启。原30秒限制被本决定替代。

## 2026-09-17 / 腕部状态查询40ms改为20ms，保持插值50ms门

连续跟随日志4次停止均为wrist_pose_time_unmatched，实际两端反馈跨度50.10～55.53ms；
光流和planner正常。源码发现RMStateReader在main入口固定40ms查询，叠加调度/网络抖动后
接近或超过50ms插值门。仅腕部点击模式改20ms查询，由RobotRuntimeConfig提供，普通模式保持40ms。
不放宽插值跨度、观测年龄或失效锁存，不改TCP/ServoJ优先级。界面将Robot具体follow_reason写入
失效原因，避免仅提示“restarted or held”。实际反馈改善与运动负载下的复验分别记录。

## 2026-09-21：80 mm 工作距离的相机启动配置

点击追踪和投影预览统一通过 `gemini_config.py` 在开流前加载设备的 Close-Range
预设，将视差搜索模式设为 SDK 枚举 2（256），读回预设及模式，不匹配即退出。
彩色 MJPG、深度 Y16 均明确请求 1280×800 @ 30 FPS，不自动降级。
使用所选彩色流内参和畸变参数继续对齐、反投影；配置写入 JSONL。
依据用户提供 Gemini 305 Minimum-Z 表，该组合下限为 50 mm，供约 80 mm
相机工作距离使用，不改变机器人 TCP 的 50 mm 悬停目标或追踪门限。
曝光等参数采用设备 Close-Range 预设；实际深度质量与处理延迟仍需实测。

## 2026-09-21：腕部跟随禁用超声 Tool-Y 参考重置

在 main_rm75 的 visual_y_tracking_reference_rebased 分支增加模式判断：
仅 wrist_follow_calibration 为空时允许原 Tool-Y 重置逻辑。腕部跟随不会因
Tool-Y 主导误差自动重置 model_joints、cartesian_reference_pose 或清零
previous_joint_delta；开始/恢复时由腕部控制器从实测状态建立参考的行为保留。
原因：先前腕部运行记录出现 27 次该重置，造成参考不连续的风险。
后续位置、关节和姿态跟踪误差保护保持原样，不以重置误差掩盖跟踪落后。
已完成生产入口和离线测试目标构建；真机抖动改善仍需运行日志验证，未启动机械臂。

## 2026-09-21：腕部位置跟踪误差门限 50 mm

按用户明确要求，将腕部跟随实测 TCP 与规划模型的位置误差门限由 25 mm 改为 50 mm。
参数属于 Rm75RuntimeSafetyConfig，由运行配置按腕部模式选择，启动输出与 summary 使用有效值。
普通超声模式仍为 25 mm，其他保护和速度限制保持不变。
此变更允许更大的跟踪偏差，不代表抖动根因消除；验证采用配置回归测试、构建与离线 CTest，未启动真实运动。

## 2026-09-21：姿态目标增加 1° 更新死区

针对静止目标下法向波动引起持续姿态修正，在 WristFollowController 中以 Base
坐标的上次接受目标旋转作为比较基准。小于等于 1° 时保留该旋转，超过时更新；
开始和恢复时重置到当前有效法向构造的姿态。仍以 5°/s 平滑参考，不改变原始
法向、点、有效性、间距计算及 50 mm 偏置，因此位置侧法向噪声仍存在。
死区内 Tool-Z 可以与瞬时负法向存在最多约 1° 目标偏差（实测姿态不作此保证）。
最新相机日志 1789958645420059791：1043 有效样本，法向帧差中位 0.325°、
P95 0.717°、最大 1.154°；平面 RMS 中位 0.231 mm、最大 0.399 mm。
这不是静止相机深度噪声验收：相机随臂运动，未保存连续原始深度用于独立判定，
不能把深度 71.9～111.4 mm 的变化直接归因于传感器故障。
增加小扰动不更新、大倾斜更新及角速度边界离线测试；真实抖动改善仍待验证。

## 2026-09-21：姿态目标更新死区调整为 5°

按用户要求，将上次接受的目标朝向与新目标之间的更新死区从 1° 改为 5°。
变化不超过 5° 保持目标朝向，超过才更新；开始/恢复初始化行为与 5°/s 角速度限制不变。
瞬时法向与接受的目标 Tool-Z 可存在约 5° 偏差；此值不是实际姿态误差保证。
回归检查调整为 4° 保持、6° 更新，并继续检查角速度上限。
